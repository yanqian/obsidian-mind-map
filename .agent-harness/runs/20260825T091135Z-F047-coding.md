# Run Record: F047 - links and screenshot export

## Summary

- Date: 2026-08-25
- Agent role: provider-native Coding Agent (`work-fast`)
- Feature: F047
- Result: implementation and local plus real-app verification passed; independent evaluation pending

## Repository State

- Starting commit: `48c7346`
- Ending commit: no commit created
- Working tree status: Harness initialization plus F044-F047 changes remain uncommitted

## Commands Run

```bash
npm run verify
./init.sh
```

## Evidence

- Links: state-free replacement handles multiple Wiki and rendered Markdown anchors in one node and remains unchanged on a second rewrite pass.
- Safety: URL query values use `encodeURIComponent`; relative Markdown hrefs are decoded once before encoding; URI-scheme and protocol-relative targets are not converted.
- Real app: the rendered fixture exposed `Linked Note`, `Aliased internal link`, and `Local Markdown link` as `obsidian://open` links while `External HTTP link` remained `example.com/mind-map`.
- Screenshot success: Obsidian 1.12.7 displayed `Screenshot copied to the clipboard.` after serializing the current view-owned SVG, drawing it to canvas, converting it to PNG, and awaiting `navigator.clipboard.write`.
- Screenshot failure: a first Blob-URL implementation tainted the Electron canvas; the rejection was caught and displayed as an `Unable to copy screenshot` Notice. The implementation then switched to a same-origin encoded SVG data URL and passed.
- Error coverage: missing SVG, missing ClipboardItem/clipboard write, missing canvas context, image decode failure, null PNG blob, and clipboard rejection all resolve to a user-visible Notice and `false` rather than an unhandled rejection.
- Screenshot: `.agent-harness/runs/F047-links-and-screenshot.png`.
- Automated checks: root recovery, TypeScript, 6 project tests, and production bundling passed.

## Failure Analysis

- Failure domain: environment_gap
- Failure summary: Blob-URL SVG decoding succeeded but tainted Electron's canvas and prevented PNG extraction.
- Harness improvement: the real-app screenshot check is retained as required evidence because source-level tests cannot predict Electron canvas origin behavior.
- Follow-up feature: F048 will repeat link/DOM/error checks against the packaged install.

## Coding Result

```text
FAST_CODING_EVIDENCE: F047
CODING_PASS: F047
```

## Follow-Up

- Rerun `make -C .agent-harness work-fast` so a separate cold-start Evaluator Agent verifies F047.
