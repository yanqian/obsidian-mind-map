# Run Record: F046 - live following, pinning, and layout state

## Summary

- Date: 2026-08-25
- Agent role: provider-native Coding Agent (`work-fast`)
- Feature: F046
- Result: implementation and local plus real-app verification passed; independent evaluation pending

## Repository State

- Starting commit: `48c7346`
- Ending commit: no commit created
- Working tree status: Harness initialization plus F044-F046 changes remain uncommitted

## Commands Run

```bash
npm run verify
./init.sh
```

## Evidence

- Event model: `file-open` follows Markdown navigation and vault `modify` refreshes the selected source; the former one-second `setInterval` path is removed.
- Pin behavior: the view action visibly changes between `Pin mind map` and `Unpin mind map`; while pinned, opening `Linked Note` kept the `Mind Map Fixture` nodes, and unpinning immediately rendered the latest opened note.
- State contract: `getState` and `setState` serialize and restore `file` plus `pinned`; the ignored test-vault workspace recorded `file: Linked Note.md` and `pinned: true`.
- Layout restart: after closing and reopening Obsidian on `Mind Map Fixture`, the right pane restored `Mind Map of Linked Note`, showed `Unpin mind map`, and retained the Linked Note nodes.
- Edit refresh: externally adding `Live event update` and `Refreshed without polling` to `Linked Note.md` caused both nodes to appear in the open preview through the vault modification event.
- Cleanup: workspace and vault event refs have emitter-specific cleanup callbacks, and SVG removal is scoped to the view container.
- Screenshot: `.agent-harness/runs/F046-pinned-layout-restore.png`.
- Automated checks: root recovery, TypeScript, 4 project tests, and production bundling passed.

## Failure Analysis

- Failure domain: none
- Failure summary: none; one interaction exposed that unpinning from the map pane must remember the latest Markdown file even while pinned, and the implementation was corrected before final verification.
- Harness improvement: the source contract test now rejects polling and requires event-driven following, serialized pin state, and explicit event cleanup.
- Follow-up feature: F048 retains final packaged-install and console verification.

## Coding Result

```text
FAST_CODING_EVIDENCE: F046
CODING_PASS: F046
```

## Follow-Up

- Rerun `make -C .agent-harness work-fast` so a separate cold-start Evaluator Agent verifies F046.
