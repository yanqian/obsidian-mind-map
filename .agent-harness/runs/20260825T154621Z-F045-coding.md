# Coding Evidence — F045

## Outcome

Fixed the production-only fenced-code crash that Human Eval found after F045's first acceptance. `markmap-lib@0.10.2` imports `prismjs/components/`, which esbuild's browser bundle resolves to Prism metadata rather than the Node-only dynamic language loader. The transform now omits the incompatible Prism plugin while retaining fenced-code content and the stable KaTeX plugin. The view also catches transformation/rendering failures and replaces stale empty-state text with an honest error state.

## Changed Surface

- `src/markdown-transform.ts`: configure the bundled Markmap transform without the incompatible Prism plugin.
- `src/mindmap-view.ts`: catch read/transform/render failures and show `Unable to render this note as a mind map`.
- `tests/markdown-transform.test.ts`: cover source and production-style browser-bundle transforms for unlabeled and `bash` fenced code.

## Verification

- `npm run release:local`: passed `tsc --noEmit`, 10 Node tests, production esbuild bundling, and local release packaging.
- Installed `manifest.json` and `main.js` SHA-256 values matched `dist/obsidian-mind-map-2.0.0/SHA256SUMS`.
- Obsidian 1.13.7 in the work vault rendered `01-Note/DDIS Note - Chapter 3.md` as a populated mind map, including the previously failing fenced Bash block.
- Obsidian Developer Console showed seven informational messages and no errors; the former `(0, Qs.default) is not a function` failure was absent.
- GUI evidence: `.agent-harness/runs/F045-fenced-code-fixed-obsidian-1.13.7.jpeg`.
- The installed `obsidian` CLI still exits 134; runtime verification therefore used the documented Computer Use fallback.
- Obsidian's `Reload app without saving` command left its existing Electron process in a white-screen state. Terminating that specific hung process and cold-starting Obsidian restored the workspace and demonstrated that the repaired plugin loads and renders normally.

FAST_CODING_EVIDENCE: F045
CODING_PASS: F045
