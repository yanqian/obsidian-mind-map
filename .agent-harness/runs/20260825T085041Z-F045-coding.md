# Run Record: F045 - current-note mind-map preview

## Summary

- Date: 2026-08-25
- Agent role: provider-native Coding Agent (`work-fast`)
- Feature: F045
- Result: implementation and local plus real-app verification passed; independent evaluation pending

## Repository State

- Starting commit: `48c7346`
- Ending commit: no commit created
- Working tree status: Harness initialization plus F044 and F045 changes remain uncommitted

## Commands Run

```bash
npm run verify
./init.sh
```

## Evidence

- Command gating: `checkCallback` exposes the command only when a Markdown source file is available.
- View activation: the command opens the registered `mindmap` view through `WorkspaceLeaf.setViewState` and includes the source file path in view state.
- Automated checks: TypeScript, 3 project tests, and the production esbuild bundle pass through `./init.sh`.
- Real app: Obsidian Desktop 1.12.7 loaded the rebuilt plugin from the ignored isolated `test-vault`.
- Rendered behavior: invoking `Mind Map: Preview the current note as a Mind Map` opened a populated split pane titled `Mind Map of Mind Map Fixture`; visible nodes included `Nested node visible`, `Front matter removed`, and `Linked Note`, with no `No content found` pane.
- Screenshot: `.agent-harness/runs/F045-obsidian-1.12.7-preview.png`.

## Failure Analysis

- Failure domain: test_environment
- Failure summary: a duplicate legacy plugin directory in the isolated vault shared the same manifest ID and shadowed the rebuilt package, so the command initially did not register.
- Harness improvement: the durable real-app evidence records both the exact plugin ID and isolated package location; future packaging work will use one canonical plugin directory.
- Follow-up feature: F048 will formalize package installation and full real-app console verification.

## Coding Result

```text
FAST_CODING_EVIDENCE: F045
CODING_PASS: F045
```

## Follow-Up

- Rerun `make -C .agent-harness work-fast` so a separate cold-start Evaluator Agent verifies F045.
