# Run Record: F048 - packaged local release verification

## Summary

- Date: 2026-08-25
- Agent role: provider-native Coding Agent (`work-fast`)
- Feature: F048
- Result: implementation and packaged real-app verification passed; independent evaluation pending

## Repository State

- Starting commit: `48c7346`
- Ending commit: no commit created
- Working tree status: Harness initialization and F044-F048 changes remain uncommitted

## Commands Run

```bash
npm run release:local
./init.sh
obsidian help
obsidian version
```

## Evidence

- Release output: `dist/obsidian-mind-map-2.0.0/` contains `manifest.json`, `main.js`, and `SHA256SUMS`; the first two files checksum-match the package copied into `test-vault/.obsidian/plugins/obsidian-mind-map/`.
- Metadata: package, lockfile, manifest, and `versions.json` declare 2.0.0 consistently; manifest minimum app version is 1.8.9.
- Documentation: README explicitly distinguishes the maintained manual fork from the unmodified upstream 1.1.0 Community Plugins entry and documents the exact plugin folder shape.
- Automated checks: root recovery, TypeScript, 8 project tests, and production bundling passed.
- Real app: the package was reloaded from the isolated plugin directory in Obsidian Desktop 1.12.7 and rendered the full fixture preview.
- DOM: DevTools returned `{version: '2.0.0', svgCount: 1}` and `{groups: 12, foreignObjects: 11}`.
- Console: after packaged reload, DevTools reported two informational messages (`Obsidian Developer Console` and `Received URL action`) and no errors.
- Visual evidence: `.agent-harness/runs/F048-packaged-preview.png`.
- DOM/console evidence: `.agent-harness/runs/F048-devtools-dom-console.png`.
- CLI capability: the installed `obsidian` CLI exits 134 with no output for both `help` and `version`; the GUI DevTools path supplied the required error and DOM evidence instead.

## Failure Analysis

- Failure domain: capability_gap
- Failure summary: the locally installed Obsidian CLI crashes before dispatching any command, so its documented `plugin:reload`, `dev:errors`, and `dev:dom` helpers were unavailable.
- Harness improvement: F048 records the failed commands and uses durable DevTools console/DOM screenshots plus filesystem checksums as an explicit fallback rather than silently skipping the verification.
- Follow-up feature: none; the fallback achieved the selected feature's verification surface.

## Coding Result

```text
FAST_CODING_EVIDENCE: F048
CODING_PASS: F048
```

## Follow-Up

- Rerun `make -C .agent-harness work-fast` so a separate cold-start Evaluator Agent verifies F048.
