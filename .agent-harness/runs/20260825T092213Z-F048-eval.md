# Run Record: F048 - packaged local release evaluation

## Summary

- Date: 2026-08-25
- Agent role: Evaluator Agent
- Feature: F048
- Result: accepted

## Repository State

- Starting commit: `48c7346`
- Ending commit: no commit created
- Working tree status: F044-F048 and harness bootstrap changes remain uncommitted

## Commands Run

```bash
./init.sh
npm run release:local
find dist -maxdepth 3 -type f | sort
find test-vault -maxdepth 4 \( -name manifest.json -o -name main.js -o -name SHA256SUMS \) | sort
shasum -a 256 dist/obsidian-mind-map-2.0.0/manifest.json dist/obsidian-mind-map-2.0.0/main.js test-vault/.obsidian/plugins/obsidian-mind-map/manifest.json test-vault/.obsidian/plugins/obsidian-mind-map/main.js
```

## Evidence

- Tests: `./init.sh` passed harness checks, 8 project tests, and the production build.
- Logs: `npm run release:local` rebuilt successfully and emitted `dist/obsidian-mind-map-2.0.0/`.
- Screenshots or traces: `.agent-harness/runs/F048-packaged-preview.png` shows the packaged plugin enabled in Obsidian Desktop 1.12.7 with a rendered mind-map pane. `.agent-harness/runs/F048-devtools-dom-console.png` shows plugin version `2.0.0`, one `.mindmap-svg`, 12 SVG groups, 11 `foreignObject` nodes, and no console errors.
- External behavior verification: `README.md` distinguishes manual fork installation from the abandoned upstream Community Plugins catalog entry and documents the direct plugin-folder layout.
- Capability gaps: the local `obsidian` CLI crash remains recorded in the coding evidence, but F048 kept durable GUI-based console and DOM evidence instead of silently skipping runtime verification.
- Packaging: the release directory contains `manifest.json`, `main.js`, and `SHA256SUMS`.
- Checksum match: the packaged `manifest.json` and `main.js` hashes exactly match the copies installed into `test-vault/.obsidian/plugins/obsidian-mind-map/`.

## Failure Analysis

- Failure domain: none
- Failure summary: none
- Harness improvement: no additional harness improvement required; the existing coding record already captured the CLI capability fallback durably.
- Follow-up feature: none

## Files Changed

- `.agent-harness/runs/20260825T092213Z-F048-eval.md`

## Evaluator Result

```text
EVAL_PASS: F048
```

## Follow-Up

- Orchestrator or maintainer may now mark `F048` complete using this evaluator evidence.
