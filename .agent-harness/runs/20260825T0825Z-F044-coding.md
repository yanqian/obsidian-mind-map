# Run Record: F044 - reproducible plugin recovery and build

## Summary

- Date: 2026-08-25
- Agent role: provider-native Coding Agent (`work-fast`)
- Feature: F044
- Result: implementation and local verification passed; independent evaluation pending

## Repository State

- Starting commit: `48c7346`
- Ending commit: no commit created
- Working tree status: Harness initialization plus F044 source/build/test changes are uncommitted

## Commands Run

```bash
npm install --no-audit --no-fund
npm run verify
./init.sh
```

## Evidence

- Tests: Node test runner passed 2 project-owned Markdown transformation tests.
- Logs: `./init.sh` completed Harness verification, dependency recovery, TypeScript validation, tests, and production build with `Plugin recovery passed`.
- Build: production esbuild generated `main.js` (approximately 417 KiB) with exit code 0.
- External behavior verification: local `codex exec --help` confirmed stdin prompt and `--ephemeral` semantics; evaluator runtime preflight passed after approved Codex-state access.
- Capability gaps: the `tsx` CLI attempted a restricted IPC socket, so the durable test command was changed to `node --import tsx --test`, which passed without elevated access.

## Failure Analysis

- Failure domain: capability_gap
- Failure summary: initial test runner command was not compatible with the restricted local sandbox.
- Harness improvement: root `./init.sh` now exercises the project test command, so future recovery detects this class of local-only runner failure.
- Follow-up feature: none; runtime preview migration remains the already-planned F045.

## Files Changed

- Build/recovery: `package.json`, `package-lock.json`, `tsconfig.json`, `esbuild.config.mjs`, `init.sh`, `.gitignore`, removed `rollup.config.js`
- Core test seam: `src/markdown-transform.ts`, `src/constants.ts`, `src/obsidian-markmap-plugin.ts`, minimal compile adjustments in `src/main.ts` and `src/mindmap-view.ts`
- Verification/docs: `tests/markdown-transform.test.ts`, `README.md`
- Harness state: `.agent-harness/SPEC.md`, `.agent-harness/feature_list.json`, `.agent-harness/progress.md`, `.agent-harness/runs/`

## Coding Result

```text
FAST_CODING_EVIDENCE: F044
CODING_PASS: F044
```

## Follow-Up

- Rerun `make -C .agent-harness work-fast` so a separate cold-start Evaluator Agent verifies F044.
